services:
  - type: web
    name: ai-ecotourism-backend
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: .
    plan: free
    region: singapore
    healthCheckPath: /
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
